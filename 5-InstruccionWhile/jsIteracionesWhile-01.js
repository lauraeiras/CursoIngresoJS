/*
al presionar el botón mostrar 10 repeticiones 
con números ASCENDENTE, desde el 1 al 10.*/
//while,iterador,bucle.
function mostrar() {

	/**
	 * contador: Variable mas una constante.
	 * contador = contador + 1;
	 * contador += 1;
	 * contador++; Solo permite sumar uno/operador 
	 * ++contador; pre-incremento;/ primero suma y despues muestra
	 * 
	 * acumulador: Variable mas otra variable.
	 * acumulador = acumulador + otraVariable;
	 * acumulador += otravariable;
	 * 
	 * contador y acumulador se inicializan siempre;
	 * 
	 * bandera:
	 * 
	 */
	/*var contador = 0;//inicializar el contador(Primero imprime y despues suma)


	while (contador < 10) {
		
		contador = contador + 1;// variable + constante.
		console.log("Contador", contador);

	}*/
	/*while()
	{
		alert("Hola repetido"); //si la logica es verdadera repite codigo
	}//<-- vuelve a la logica*/


	/*var numeroIngresado;
	var contador;
	var acumulador;
	var suma;
	var promedio;

	// es necesario inicializar estas variables
	contador = 0;// 1er parte aseguro el ingreso
	acumulador=0;

	while(contador<3)// 2da parte aseguro el ingreso minimo una vez
	{
		numeroIngresado=prompt("Ingrese numero");
		numeroIngresado=parseInt(numeroIngresado);
		acumulador=acumulador+numeroIngresado;
		contador=contador+1;// 3ra modifico el valor para salir.
	}
	//CONTADOR =3 en esta linea tiene el valor que le asigne en while.
	console.log("salio del while con :"+contador);
	promedio=suma/3;// promedio=suma/contador; lo pongo fuera porque solo lo necesito una vez*/

	/*var clave;
	do
	{
		clave=prompt("Ingrese clave");
	}while(clave!= "clave secreta");*/

	var contador;
	var edad;
	var sexo;
	var temperatura;
	contador=0;

}//FIN DE LA FUNCIÓN