function mostrar()
{
	/*var contador;
	contador = 0;

	while(contador < 3){
		contador++;
	}
	//recomendable usar cuando sabemos cantidad de iteraciones.
	//variable local solo del for/no garantiza que se ejecute una vez
	for(var contador = 0; contador < 3;contador++){
		console.log(contador);
	}
	// me pide que ponga los ;
	for(;;){

	}*/
	for(var contador = 1;contador < 11;contador++){
		document.write(contador);
	}
	
}